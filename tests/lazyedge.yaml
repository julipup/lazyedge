# Deployment configurations
deploy:
  namespace: "knative-functions"
  registry: "registry.odzi.dog/lazyedge-tests"
  builder:
    type: "s2i"
    platform: "linux/arm64"

# Routes configurations
routes:
  - name: "test"
    entrypoint: "./routes/test.ts"
  - name: "test2"
    entrypoint: "./routes/test2.ts"
